<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network access for cloud sync -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- FIX (2026-08-06): auto local backup → public Documents (survives uninstall) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Play Billing (M8.4) -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- WorkManager / Sync Service -->
    <!-- tools:ignore: Android 14+ type requirement satisfied via FOREGROUND_SERVICE_DATA_SYNC
         permission + SystemForegroundService declared with foregroundServiceType="dataSync" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicePermission" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Bluetooth (for ESC/POS and Label printers — M5) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Camera (ML Kit scanner — M5, M6) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- FIX 1: Camera uses-feature required — prevents install on devices without camera
         required="false" → app still installable on no-camera devices (tablets/desktops) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Vibration (haptic feedback on scan) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Notifications (sync status) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TillzoPOSApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TillzoPOS"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <!-- Suppress 16KB alignment check in debug builds only -->
        <meta-data
            android:name="com.android.graphics.EGL_SWAP_BEHAVIOR_PRESERVED"
            android:value="true" />

        <!-- FIX 2: screenOrientation removed from manifest (triggers Android 16 + Chrome OS warnings).
             Portrait lock is enforced in MainActivity.onCreate() via requestedOrientation. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FIX 3: WorkManager foreground service type (Android 14+ requirement).
             foregroundServiceType="dataSync" must be declared for FOREGROUND_SERVICE_DATA_SYNC. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:node="merge" />

        <!-- WorkManager: disable auto-init (using Configuration.Provider pattern).
             Default WorkManagerInitializer ContentProvider runs BEFORE Hilt can inject
             HiltWorkerFactory → UninitializedPropertyAccessException → 20% crash.
             WorkManager initializes lazily on first use, by which time Hilt is ready. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>
</manifest>
