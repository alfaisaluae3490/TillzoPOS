# TillzoPOS — Production-Ready Testing Protocol for Hermes
**Repo:** `alfaisaluae3490/TillzoPOS` | **Stack:** Kotlin, Jetpack Compose, Room (local), Google Sheets REST API v4 (remote sync) | **Architecture:** Clean Architecture — UI → ViewModel → UseCase → Repository → (Room DAO + SheetsRemoteDataSource)

This protocol was generated by scanning the actual source tree (`app/src/main/java/com/tillzo/pos/...`), not by generic assumption. Every field name, button label, table name, and sheet column below is copied verbatim from the code (Compose `label = Text("...")`, `Button { Text("...") }`, and each Room entity's `toSyncMap()` function, which is the literal payload sent to Google Sheets). Where a screen's dropdown/enum values were found in source, they are listed explicitly.

**Rule for Hermes:** Do not paraphrase field names. Type test data into the field using the *exact* label text given below. When verifying Google Sheets, check the *exact* column key given below (these keys become the header row of each sheet tab, since `tableName` in `@Entity(tableName = "X")` is the sheet tab name, and each JSON key in `toSyncMap()` is the column header).

---

## PHASE 1 — MICRO-MAPPING MATRIX

### 1.A Google Sheets Tabs (`tableName`) → Exact Columns (from `toSyncMap()`)

| # | Room Table / Sheet Tab | Entity File | Exact Sheet Columns (in code order) |
|---|---|---|---|
| 1 | `Inventory` | InventoryEntity.kt | `system_row_id, item_number, barcode_id, name, category, unit, selling_price, price, stock_qty, low_threshold, sku, brand, description, cost_price, tax_percent, batch_number, expiry_date, manufacturing_date, expiry_alert_days, is_damaged, damaged_qty, total_stock, has_batches, is_deleted, deleted_at, last_updated, created_at, updated_at, pos_terminal_id` |
| 2 | `Categories` | CategoryEntity.kt | `system_row_id, category_name, parent_category_id, is_deleted, deleted_at, sync_status, pos_terminal_id, created_at, updated_at` |
| 3 | `Customers` | CustomerEntity.kt | `system_row_id, name, phone, whatsapp, email, address, loyalty_points, lifetime_spend, is_deleted, deleted_at, sync_status, pos_terminal_id, created_at, updated_at` |
| 4 | `KhataEvents` | KhataEventEntity.kt | `event_id, system_row_id, customer_id, pos_id, event_type, type, amount, note, reference_sale_id, sync_uuid, is_deleted, deleted_at, last_updated, sync_status, created_at, updated_at, pos_terminal_id` |
| 5 | `Expenses` | ExpenseEntity.kt | `system_row_id, category, amount, description, timestamp, logged_by_user_id, is_deleted, deleted_at, sync_status, pos_terminal_id, created_at, updated_at` |
| 6 | `grn_headers` | GrnEntity.kt (GrnHeaderEntity) | `grn_id, grn_number, po_id, po_number, vendor_id, vendor_name, vendor_phone, status, notes, received_by, received_by_name, total_items, total_received_qty, total_amount, sync_status, is_deleted, deleted_at, pos_terminal_id, attached_file_id, attached_file_url, created_at, updated_at` |
| 7 | `grn_items` | GrnEntity.kt (GrnItemEntity) | `grn_item_id, grn_id, po_item_id, product_id, batch_id, product_name, barcode_id, sku, category_id, brand, ordered_qty, received_qty, unit_cost_price, selling_price, total_cost, unit, batch_number, manufacturing_date, expiry_date, inventory_action, is_new_product, low_stock_threshold, sync_status, created_at, updated_at` |
| 8 | `product_batches` | ProductBatchEntity.kt | `batch_id, product_id, barcode_id, batch_number, manufacturing_date, expiry_date, stock_qty, cost_price, selling_price, is_active, is_deleted, deleted_at, sync_status, pos_terminal_id, created_at, updated_at` |
| 9 | `product_units` | ProductUnitEntity.kt | `unitId, unitName, abbreviation, isDeleted, syncStatus, createdAt, updatedAt` |
| 10 | `purchase_orders` | PurchaseOrderEntity.kt (PurchaseOrderEntity) | `po_id, po_number, vendor_id, vendor_name, status, notes, total_amount, currency, expected_delivery_date, created_by, sync_status, pos_terminal_id, created_at, updated_at` |
| 11 | `purchase_order_items` | PurchaseOrderEntity.kt (PurchaseOrderItemEntity) | `po_item_id, po_id, product_id, product_name, sku, barcode_id, ordered_qty, received_qty, unit_cost_price, total_cost, unit, sync_status, created_at, updated_at` |
| 12 | `vendors` | PurchaseOrderEntity.kt (VendorEntity) | `vendor_id, name, phone, whatsapp, email, address, city, credit_limit, is_active, is_deleted, sync_status, created_at, updated_at` |
| 13 | `Sales` | SaleEntity.kt | `invoice_id, pos_id, timestamp, items_json, subtotal, tax, discount, total, payment_method, payment_split_json, cash_amount, card_amount, wallet_amount, udhaar_amount, customer_id, reference_id, cashier_id, sync_uuid, is_deleted, deleted_at, last_updated, sync_status, pos_terminal_id, system_row_id, created_at, updated_at` |
| 14 | `Time_Clock` | TimeClockEntity.kt | `system_row_id, employee_email, employee_name, event_type, timestamp, note, pos_terminal_id, created_at, updated_at, sync_status` |
| 15 | `Users_Permissions` | UserEntity.kt | `system_row_id, email, name, role, password_hash, permissions_json, is_deleted, deleted_at, sync_status, pos_terminal_id, created_at, updated_at` |
| 16 | `StockAdjustments` | StockAdjustmentEntity.kt | *(no `toSyncMap`; raw fields)* `adjustmentId, productId, adjustmentType, quantityChanged, reason, adjustedBy, syncStatus, createdAt, updatedAt, posTerminalId` |
| 17 | `till_sessions` | TillSessionEntity.kt | *(no `toSyncMap`; raw fields, comment confirms "Till_Sessions tab")* `sessionId, cashierId, cashierName, posTerminalId, openingCash, closingCash, expectedCash, totalCashSales, totalCardSales, totalWalletSales, totalUdhaarSales, totalSplitSales, totalSalesCount, totalRefunds, netCash, cashVariance, totalPayIn, totalPayOut, status, notes, shiftDate, openedAt, closedAt, syncStatus, posId, createdAt, updatedAt` |
| 18 | `wastage_log` | WastageEntity.kt | via `toSheetRow()`: `wastageId, productId, productName, batchId, batchNumber, quantity, unit, costPrice, totalLoss, reason, notes, loggedBy, wastageDate, syncStatus, posTerminalId, createdAt, updatedAt` |
| 19 | `app_logs` | AppLogEntity.kt | local-only (device log viewer; not synced to Sheets) |

### 1.B Google Sheets / Drive REST Endpoints (`SheetsApiService.kt`)

| # | Method | Endpoint | Purpose |
|---|---|---|---|
| 1 | POST | `spreadsheets` | `createSpreadsheet` — onboarding, new workbook |
| 2 | GET | `spreadsheets/{spreadsheetId}/values/{range}` | `getValues` — pull rows |
| 3 | POST | `spreadsheets/{spreadsheetId}/values/{range}:append` (`valueInputOption=RAW`, `insertDataOption=INSERT_ROWS`) | `appendValues` — CREATE |
| 4 | POST | `spreadsheets/{spreadsheetId}/values:batchUpdate` | `batchUpdate` — UPDATE cell values |
| 5 | POST | `spreadsheets/{spreadsheetId}:batchUpdate` | `sheetsBatchUpdate` — structural: add/rename/hide tab |
| 6 | GET | `spreadsheets/{spreadsheetId}` (`fields=sheets.properties`) | `getSpreadsheet` — schema/tab metadata check |
| 7 | GET | `https://www.googleapis.com/drive/v3/files` | `searchDriveFiles` — locate workbook by `appProperties` |
| 8 | POST | `https://www.googleapis.com/drive/v3/files` | `createDriveFolder` |
| 9 | PATCH | `https://www.googleapis.com/drive/v3/files/{fileId}` | `updateAppProperties` |

### 1.C UI Screens → Exact Field Labels, Buttons, Dropdowns, Toggles

> Format: **Screen file** — Fields (`label = Text("...")` exactly as coded) — Buttons (exact button text) — Dropdown/enum values (exact) — Switches/Checkboxes count.

**1. `ui/signin/SignInScreen.kt`**
- Buttons: `Continue with Google`, `Try Again`, `Accept & Sync`, `Cancel`

**2. `ui/signin/SheetSelectionScreen.kt`**
- 1 primary button (sheet confirmation)

**3. `ui/setup/SheetPickerScreen.kt`**
- Buttons: `Retry`, `Retry Restore`

**4. `ui/setup/onboarding/OnboardingScreen.kt`**
- Buttons: `Back` (OutlinedButton), plus forward/continue action

**5. `ui/auth/options/session/PINUnlockScreen.kt`**
- Fields: `Enter New 4-Digit PIN`, `Enter 4-Digit PIN`
- Buttons: `Save PIN`, `Unlock`, `Logout & Login via OAuth`

**6. `ui/security/RootBlockedScreen.kt`**
- Button: `EXIT APP`

**7. `ui/security/AdminAndUsersScreen.kt`** (Users_Permissions CRUD)
- Fields: `Email *`, `Display Name`
- Buttons: `← Back`, `+ Add User`, `🗑` (IconButton, delete user), `Role: $role` (OutlinedButton — role picker), `Save`, `Cancel`, `Users`
- 2 dropdowns (role selector context)

**8. `ui/home/HomeScreen.kt`** (POS sale screen — largest screen, 28 button call-sites)
- Search: placeholder `Search by name, SKU, or scan barcode…`
- Fields: `Qty (${item.unit})` (cart line quantity), `New 4-digit PIN`, `Confirm PIN`, `Enter 4-digit PIN`, `Amount ($currencySymbol)`, `Amount (Rs)`, `Item Name / Note` (custom/manual item), `Selling Price (Rs)` (custom item price)
- Placeholders: `e.g. 500`, `e.g. Custom Shirt`, `e.g. 15.50`
- Buttons: `Remove`, `Clear Cart`, `Clear`, `Cancel`, `Add to Cart`, `Apply` (discount), `Confirm`, `Update`
- 2 dropdown menus, 5 icon buttons

**9. `ui/home/PaymentDialog.kt`** (checkout / split payment)
- Fields: `Search customer name or phone` (×2 instances), `Credit Amount`, `Name *`, `Phone *`, `WhatsApp (optional)`
- Payment method fields (from `PaymentMethod` enum, verified in code): **`Cash`, `Card`, `Digital Wallet`, `UDHAAR`** (Khata/credit)
- Buttons: `Add New Customer` (×2), `Save & Select Customer`
- 2 switches, 1 icon button

**10. `ui/home/ReceiptScreen.kt`**
- Field: `WhatsApp Number (with country code)`
- 4 action buttons (Print / Share / WhatsApp / Done type actions)

**11. `ui/home/AdvancedMenuSheet.kt`** (nav-drawer to all modules — exact entries)
`Wastage Entry`, `Returns & Refunds`, `CRM / Accounts`, `Inventory`, `Vendors`, `Stock Adjustment`, `Stock Alerts`, `Purchase Orders`, `Goods Receipts`, `Transaction History`, `Force Sync`, `Z-Report / Day Close`, `Expenses`, `Open / Close Register`, `Time Clock`, `Verify Receipt QR`, `Admin Dashboard`, `Hardware Diagnostics`, `Settings`

**12. `ui/inventory/options/crud/InventoryCrudScreen.kt`** (Inventory CRUD — most complex form)
- Filter chips: `⚠️ $lowCount Low`, `🚫 $outCount Out`, `⏰ $expiringCount Expiring`
- Search placeholder: `Search items...`
- Fields: `New Category`, `Product Name *`, `SKU *`, `Main Category *`, `Subcategory`, `Description`, `Add GTIN (Leave blank for Auto)`, `Cost Price`, `Selling Price`, `Tax %`, `Current Stock`, `Low Alert`, `Unit (KG/ML/PC)`, `Batch Number *`, `Mfg Date`, `Exp Date *`, `Expiry Alert (Days Before)`, `Damaged Qty`
- Dropdowns: **Main Category** (`ExposedDropdownMenuBox`, item source = Categories sheet, with a special `Manage Categories...` menu item that opens the Category Manager), **Subcategory** (filtered children of selected main category), **Unit** dropdown
- Buttons: `View Batches (${item.totalStock})`, `Cancel`, `Close`, `Generate` (GTIN/barcode), `Cancel`
- 1 switch (likely "is damaged stock" toggle), 7 icon buttons, 5 total dropdown menus

**13. `ui/inventory/CategoryManagementScreen.kt`**
- Fields: `Category Name *`, `Parent Category`
- Buttons: `Delete`, `Cancel`, `Save`, `Cancel`
- 2 dropdowns (parent category picker context), 3 icon buttons

**14. `ui/inventory/ProductUnitsScreen.kt`**
- Fields: `Unit Name (e.g. Packet)`, `Abbreviation (e.g. PKT)`
- Buttons: `Cancel` (+ implicit Save/Add), 3 icon buttons (edit/delete per row)

**15. `ui/inventory/StockAdjustmentScreen.kt`**
- Fields: `Search by name, SKU, or barcode`, `Adjustment Type` (dropdown), `Quantity Change (use − for negative)`, `Reason *`
- **Adjustment Type exact dropdown values (code constant `ADJUSTMENT_TYPES`):** `RECEIVED` ("Received stock"), `CORRECTION` ("Stock correction"), `DAMAGED` ("Damaged / wastage"), `RETURNED` ("Customer return"), `SALE_RETURN` ("Sale return")
- Buttons: `Select`, `Change`

**16. `ui/inventory/options/alerts/StockAlertsScreen.kt`**
- Read-only alert list; 1 icon button (likely refresh/filter)

**17. `ui/inventory/options/ocr/OcrEntryScreen.kt`**
- Camera-based OCR entry; 1 icon button

**18. `ui/inventory/options/qr/QrGeneratorScreen.kt`**
- QR generation; 1 icon button

**19. `ui/inventory/options/crud/BarcodePrintSettingsScreen.kt`**
- Fields: `Print Quantity`, `Currency`, `Width (e.g., 144)`, `Height (e.g., 72)`, `Company Name`, `AI Name (e.g. Additional SKU)`, `AI Code (e.g. 240)`, `Value (Static text)`, `Prefix Value`, `Position`, `Suffix Value`, `Position`
- Buttons: `Delete`, `Add Custom Application Identifier (AI)`, `Cancel`, `Restore Default Settings`
- 1 switch, 8 checkboxes, 3 icon buttons

**20. `ui/inventory/options/wastage/WastageLogScreen.kt`** (Wastage Entry)
- Filter: `All`
- Fields: `Search Product`, `Reason` (dropdown), `Quantity ($selectedUnit)`, `Notes (optional)`
- **Reason exact enum values (`WastageReason`):** `EXPIRED`, `DAMAGED`, `THEFT`, `OTHER`
- Button: `Cancel` (+ implicit Save)

**21. `ui/inventory/module_a/BatchListBottomSheet.kt`** (Product Batches)
- View-batch fields: `Batch Number`, `Mfg Date (YYYY-MM-DD)`, `Expiry Date (YYYY-MM-DD)`, `Stock Qty`, `Selling Price`
- Add-batch fields: `Batch Number (optional)`, `Mfg Date (YYYY-MM-DD)`, `Expiry Date (YYYY-MM-DD)`, `Stock Qty *`, `Cost Price`, `Selling Price`
- Buttons: `+ Batch`, `Save` (×2 contexts), `Cancel` (×2 contexts)

**22. `ui/inventory/module_b/VendorManagementScreen.kt`**
- Search placeholder: `Search vendors...`
- Fields: `Name *`, `Phone *`, `WhatsApp`, `Email`, `Address`, `City`, `Credit Limit`
- Buttons: `Delete`, `Cancel` (×2)
- 1 switch (`is_active` toggle), 3 icon buttons

**23. `ui/inventory/module_b/CreatePurchaseOrderScreen.kt`**
- Fields: `Vendor Name *`, `Phone *`, `WhatsApp (optional)`, `Search or type vendor name`, `Expected Delivery Date (YYYY-MM-DD)`, `Notes (optional)`, `Search inventory to add item...`, `Qty`, `Cost`
- Buttons: `Cancel`, `OK`, `Cancel`, `Save Draft` (OutlinedButton), `Add New Vendor`
- 2 icon buttons

**24. `ui/inventory/module_b/PurchaseOrderListScreen.kt`**
- 2 buttons (create/filter), 1 icon button

**25. `ui/inventory/module_b/PODetailScreen.kt`**
- 4 buttons (status transitions e.g. Approve/Receive/Cancel), 2 icon buttons

**26. `ui/inventory/module_c/CreateGrnScreen.kt`** (Goods Receipt from PO)
- Fields: `Delivery Notes`, `Received Qty`, `Category`, `Brand`, `Selling Price`, `Batch / Lot Number`, `Mfg Date`, `Exp Date`
- 1 dropdown (Category), 2 icon buttons, 4 buttons

**27. `ui/inventory/module_c/GrnListScreen.kt`**
- Button: `Back`, 1 icon button

**28. `ui/inventory/module_c/GrnDetailScreen.kt`**
- 2 buttons, 1 icon button

**29. `ui/inventory/module_c/GrnSuccessScreen.kt`**
- Button: `Return to PO List`

**30. `ui/store/options/crm/CrmScreen.kt`** (CRM / Accounts / Khata)
- Fields: `Search Customers`, `Amount`, `Note (Optional)`
- Buttons: `WhatsApp Statement`, `Add Credit (-)`, `Record Payment (+)`, `Save` (×2 contexts), `Cancel` (×2)
- 4 icon buttons

**31. `ui/store/options/expense/ExpenseScreen.kt`**
- Fields: `Amount`, `Description`
- Button: `Cancel` (+ implicit Save), 4 icon buttons

**32. `ui/store/options/history/HistoryScreen.kt`** (Transaction History)
- Field: `Search by Invoice ID or QR UUID`
- Buttons: `Dismiss`, `Load More`, 2 icon buttons

**33. `ui/store/options/returns/ReturnsScreen.kt`**
- Field: `Scan QR or Enter Invoice UUID`
- Buttons: `Dismiss`, `Return to Inventory`, `Mark as Wastage`, `Issue Refund`, `Cancel`, 1 icon button

**34. `ui/store/options/statement/StatementScreen.kt`**
- 2 buttons, 1 icon button

**35. `ui/store/options/timeclock/PunchClockScreen.kt`**
- 2 buttons (Clock In / Clock Out), 1 icon button

**36. `ui/store/options/verifyqr/VerifyQrScreen.kt`**
- 2 buttons, 2 icon buttons

**37. `ui/store/options/zreport/ZReportScreen.kt`** (Day Close)
- Fields: `Physical Cash Count ($currencySymbol)` (×2 render points), `Opened At` (readonly), `Opening Cash` (readonly), `Cash Sales` (readonly), `Expected in Drawer` (readonly), `Pay In (Added)`, `Pay Out (Removed)`, `Total Transactions` (readonly)
- Buttons: `CLOSE DAY (Z-REPORT)`, `Cancel`, 1 icon button

**38. `ui/till/TillOpenScreen.kt`** (Open/Close Register)
- Fields: `Opening Cash (Amount)`, `Notes (optional)`
- 1 button (Open Register)

**39. `ui/hardware/HardwareDiagnosticScreen.kt`**
- 2 buttons, 1 icon button

**40. `ui/hardware/printer/PrinterSettingsScreen.kt`**
- Fields: `MAC Address (e.g. 00:11:22:33:44:55)`, `IP Address (e.g. 192.168.1.100)`
- Buttons: `Back to POS`, `Test ML Scanner`, `Test Bluetooth Connection`, `Test Network Connection`, `Hardware Diagnostics`

**41. `ui/hardware/scanner/BarcodeScannerScreen.kt`**
- Camera scanner; 2 buttons, 2 icon buttons

**42. `ui/settings/options/privacy/SettingsScreen.kt`** (main Settings)
- Fields: `Sheet ID or URL`, `Folder name`, `New 4-Digit PIN`, `Current PIN`, `New PIN`, `Confirm New PIN`
- Buttons: `Manage PIN`, `Connect`, `Cancel` (×4 dialog contexts), `Remove PIN`
- 4 switches, 1 dropdown, 1 icon button

**43. `ui/settings/options/billing/BillingScreen.kt`**
- Button: `Subscribe with Google Play`, 1 icon button

**44. `ui/settings/options/dataviewer/LocalDataViewerScreen.kt`**
- 1 button, 1 icon button (raw Room table viewer)

**45. `ui/settings/options/logviewer/SystemLogsScreen.kt`**
- Filter chip: `ALL`
- Field placeholder: `Filter by message...`
- Button: `Apply`, 3 icon buttons (5 total buttons)

**46. `ui/update/ForceUpdateScreen.kt`**
- Button: `Later` (OutlinedButton), 3 total buttons (incl. Update Now)

---
*(Continued in Phase 2 / Phase 3 below — this matrix is the source of truth Hermes must reference by exact string for every step that follows.)*

## PHASE 2 — LINE-BY-LINE HERMES TESTING PROTOCOL (CRUD per Module)

> For every module below, Hermes must execute Field/Variable Level Test → CREATE & SYNC → READ & SYNC → UPDATE & SYNC → DELETE & SYNC, in that literal order, using the exact field labels and exact sheet columns from Phase 1. Do not skip a module. Do not summarize a module's fields as "etc."

### Module 1 — Inventory (Screen: `InventoryCrudScreen.kt` | Sheet Tab: `Inventory`)

- **Field/Variable Level Test:** Open the `+ Add Item` flow. Input test data into every field in this exact order: `Product Name *` = "TEST-ITEM-01", `SKU *` = "SKU-TEST-01", `Main Category *` = select an existing category from the dropdown (or tap `Manage Categories...` first if the list is empty), `Subcategory` = select a child of the chosen main category, `Description` = "Automated test description", `Add GTIN (Leave blank for Auto)` = leave blank first, `Cost Price` = "10.50", `Selling Price` = "19.99", `Tax %` = "5", `Current Stock` = "100", `Low Alert` = "10", `Unit (KG/ML/PC)` = select "PC" from the unit dropdown, `Batch Number *` = "BATCH-TEST-01", `Mfg Date` = today's date, `Exp Date *` = a date 1 year in the future, `Expiry Alert (Days Before)` = "30", `Damaged Qty` = "0".
- **CREATE & SYNC:** Tap `Save`/`Generate` (the confirming action button on this form). Verify a new row appears in the `Inventory` sheet tab where `name` = "TEST-ITEM-01", `sku` = "SKU-TEST-01", `category` matches the selected category, `price` and `selling_price` = 19.99, `cost_price` = 10.5, `tax_percent` = 5, `stock_qty` = 100, `low_threshold` = 10, `unit` = "PC", `batch_number` = "BATCH-TEST-01", `expiry_date` and `manufacturing_date` match the entered dates, `expiry_alert_days` = 30, `is_damaged` = 0, `damaged_qty` = 0, `barcode_id` is auto-populated (was left blank), `pos_terminal_id` matches this device's terminal ID, and `is_deleted` = 0.
- **READ & SYNC:** Force-close and reopen the app (or pull-to-refresh the Inventory list). Verify "TEST-ITEM-01" renders correctly in the Inventory list grid with the correct price, stock badge, and — because `current_stock` (100) is above `low_stock_threshold` (10) — it must NOT appear under the `⚠️ Low` filter chip count.
- **UPDATE & SYNC:** Open "TEST-ITEM-01", change only `Selling Price` to "24.99" and `Current Stock` to "5". Save. In the `Inventory` sheet, verify ONLY the `selling_price`/`price` cell and `stock_qty` cell changed for that row — `system_row_id`, `sku`, `category`, `cost_price`, and all other columns for that row must remain unchanged. Verify `updated_at` changed but `created_at` did not.
- **DELETE & SYNC:** Delete "TEST-ITEM-01" from the app. Verify in the `Inventory` sheet that the row's `is_deleted` column flips to `1` and `deleted_at` is populated with a timestamp (this app uses soft-delete, not physical row removal — confirm the row still physically exists in the sheet with `is_deleted=1`). Verify the item no longer appears in the app's Inventory list or in POS search on `HomeScreen`.
- **Sub-flow — Low Stock / Out of Stock / Expiring filter chips:** Create one item with `Current Stock` = 0 (verify it counts under `🚫 Out`), one item with `Current Stock` below its `Low Alert` value (verify it counts under `⚠️ Low`), and one item with `Exp Date *` within `expiry_alert_days` of today (verify it counts under `⏰ Expiring`). Tap each chip and verify only the matching items are filtered in.
- **Sub-flow — Product Batches (`BatchListBottomSheet.kt`):** On an item with `hasBatches` = true, tap `View Batches (${item.totalStock})`. Field-level test: input `Batch Number (optional)`, `Mfg Date (YYYY-MM-DD)`, `Expiry Date (YYYY-MM-DD)`, `Stock Qty *`, `Cost Price`, `Selling Price`, tap `+ Batch`, then `Save`. Verify a new row is created in the `product_batches` sheet tab with matching `batch_number`, `manufacturing_date`, `expiry_date`, `stock_qty`, `cost_price`, `selling_price`, and that the parent Inventory row's `total_stock` recalculates to include the new batch quantity. Delete the batch and verify `is_deleted`/`is_active` flips in `product_batches` and parent `total_stock` decreases accordingly.

### Module 2 — Category Management (Screen: `CategoryManagementScreen.kt` | Sheet Tab: `Categories`)

- **Field/Variable Level Test:** Input `Category Name *` = "TEST-CAT-01". Leave `Parent Category` unset for a top-level category.
- **CREATE & SYNC:** Tap `Save`. Verify a new row in `Categories` with `category_name` = "TEST-CAT-01" and `parent_category_id` empty.
- **READ & SYNC:** Return to `InventoryCrudScreen.kt` → open `Main Category *` dropdown → verify "TEST-CAT-01" appears in the list (confirms cross-screen read-sync).
- **UPDATE & SYNC:** Rename "TEST-CAT-01" to "TEST-CAT-01-RENAMED" via the edit icon. Verify only `category_name` updates in the `Categories` sheet row and `system_row_id` is unchanged. Then re-check any Inventory item previously assigned to this category still resolves correctly to the renamed category name.
- **DELETE & SYNC:** Create a second category "TEST-CAT-CHILD" with `Parent Category` = "TEST-CAT-01-RENAMED" (tests parent-child linkage: `parent_category_id` must equal the parent's `system_row_id`). Then tap `Delete` on "TEST-CAT-01-RENAMED" and confirm via the `Delete`/`Cancel` dialog. Verify `is_deleted` = 1 for that row in `Categories`. **Edge case:** verify what happens to "TEST-CAT-CHILD" and to any Inventory item still referencing the deleted parent — does the app block deletion of a category with children/items attached, or does it orphan them? Document actual behavior.

### Module 3 — Product Units (Screen: `ProductUnitsScreen.kt` | Sheet Tab: `product_units`)

- **Field/Variable Level Test:** Input `Unit Name (e.g. Packet)` = "Test Packet", `Abbreviation (e.g. PKT)` = "TPK".
- **CREATE & SYNC:** Save. Verify `product_units` sheet has a new row with `unitName` = "Test Packet", `abbreviation` = "TPK".
- **READ & SYNC:** Verify "TPK"/"Test Packet" is selectable wherever unit pickers appear (Inventory CRUD unit dropdown, Stock Adjustment, GRN).
- **UPDATE & SYNC:** Edit the abbreviation to "TPK2" via the row's edit icon button. Verify only `abbreviation` changes in the sheet row.
- **DELETE & SYNC:** Tap the delete icon button for "Test Packet". Verify `isDeleted` = 1 in `product_units` and the unit disappears from all unit-picker dropdowns app-wide.

### Module 4 — Stock Adjustment (Screen: `StockAdjustmentScreen.kt` | Sheet Tab: `StockAdjustments`)

- **Field/Variable Level Test:** Search for an existing product via `Search by name, SKU, or barcode`, tap `Select`. Open `Adjustment Type` dropdown and verify all five exact values are present and selectable: `RECEIVED` ("Received stock"), `CORRECTION` ("Stock correction"), `DAMAGED` ("Damaged / wastage"), `RETURNED` ("Customer return"), `SALE_RETURN` ("Sale return"). Select `CORRECTION`. Input `Quantity Change (use − for negative)` = "-5" and `Reason *` = "Automated test shrinkage".
- **CREATE & SYNC:** Submit. Verify a new row in `StockAdjustments` with `adjustmentType` = "CORRECTION", `quantityChanged` = -5, `reason` = "Automated test shrinkage", `adjustedBy` = the logged-in user, `productId` matching the selected product's ID. Verify the parent product's `current_stock` in the `Inventory` sheet decreased by exactly 5.
- **READ & SYNC:** Navigate away and back; verify the adjustment history (if listed on this screen) still shows the entry after a fresh data pull.
- **UPDATE & SYNC:** **Edge case, not a normal flow** — confirm whether this screen allows editing a past adjustment at all. If no edit UI exists, mark this row as "No UPDATE path — write-once ledger by design" rather than skipping the test silently.
- **DELETE & SYNC:** Confirm whether any delete affordance exists for a posted adjustment. If none, verify this is enforced (no delete icon/swipe action present) — a financial/stock ledger should not be freely deletable. Log this as expected behavior, not a bug, only if a matching lack of a delete control is present in the code (`StockAdjustmentScreen.kt` — Hermes should double-check for a hidden long-press or swipe delete gesture before concluding).
- **Repeat the CREATE step for each of the other four adjustment types** (`RECEIVED`, `DAMAGED`, `RETURNED`, `SALE_RETURN`) with distinct positive/negative quantities and verify `current_stock` in `Inventory` moves in the correct direction for each type (e.g., `RECEIVED` and positive `RETURNED`/`SALE_RETURN` should increase stock; `DAMAGED` and negative `CORRECTION` should decrease stock).

### Module 5 — Stock Alerts (Screen: `StockAlertsScreen.kt`, read-only view of `Inventory`)

- **Field/Variable Level Test:** No input fields — this is a derived/read-only list.
- **READ & SYNC:** Set one Inventory item's `current_stock` below `low_stock_threshold` directly (via Module 1's Update flow) and confirm it appears on `StockAlertsScreen`. Set another item's `current_stock` to 0 and confirm it appears with an "out of stock" state distinct from "low stock."
- Not applicable: CREATE/UPDATE/DELETE (screen has no write path of its own — verify no hidden write actions exist beyond the 1 icon button).

### Module 6 — Vendor Management (Screen: `VendorManagementScreen.kt` | Sheet Tab: `vendors`)

- **Field/Variable Level Test:** Input `Name *` = "TEST-VENDOR-01", `Phone *` = "+971500000001", `WhatsApp` = "+971500000001", `Email` = "testvendor@example.com", `Address` = "123 Test St", `City` = "Dubai", `Credit Limit` = "5000". Toggle the `is_active` switch on.
- **CREATE & SYNC:** Save. Verify new row in `vendors` sheet: `name`, `phone`, `whatsapp`, `email`, `address`, `city`, `credit_limit` = 5000, `is_active` = 1.
- **READ & SYNC:** Go to `CreatePurchaseOrderScreen.kt` → `Search or type vendor name` → confirm "TEST-VENDOR-01" is discoverable and auto-fills `Vendor Name *`, `Phone *` when selected.
- **UPDATE & SYNC:** Edit `Credit Limit` to "7500" only. Verify only the `credit_limit` cell updates in `vendors`; `phone`, `email`, `address` unchanged.
- **DELETE & SYNC:** Tap `Delete`, confirm via dialog. Verify `is_deleted` = 1 in `vendors`. Then re-open `VendorManagementScreen` search — confirm the deleted vendor no longer appears in the active list. Also verify toggling the `is_active` switch OFF (without deleting) sets `is_active` = 0 in the sheet but keeps `is_deleted` = 0 — confirm the app treats "inactive" and "deleted" as distinct states in vendor pickers (e.g., an inactive-but-not-deleted vendor should not appear as selectable in `CreatePurchaseOrderScreen`).

### Module 7 — Purchase Orders (Screens: `CreatePurchaseOrderScreen.kt`, `PurchaseOrderListScreen.kt`, `PODetailScreen.kt` | Sheet Tabs: `purchase_orders`, `purchase_order_items`)

- **Field/Variable Level Test:** On `CreatePurchaseOrderScreen.kt`: use `Search or type vendor name` to select "TEST-VENDOR-01" (auto-fills `Vendor Name *`, `Phone *`; manually add `WhatsApp (optional)`). Input `Expected Delivery Date (YYYY-MM-DD)` = a date one week out, `Notes (optional)` = "Automated PO test". Use `Search inventory to add item...` to add "TEST-ITEM-01", set line `Qty` = "20" and `Cost` = "9.50". If the vendor doesn't exist yet, test the `Add New Vendor` inline path instead.
- **CREATE & SYNC:** Tap `Save Draft` first — verify a new row appears in `purchase_orders` with `status` = the draft status string used by the app, `vendor_id`/`vendor_name` matching, `total_amount` = 190.00 (20 × 9.50), `expected_delivery_date` matching, and a matching child row in `purchase_order_items` with `product_name` = "TEST-ITEM-01", `ordered_qty` = 20, `unit_cost_price` = 9.50, `total_cost` = 190.00, `received_qty` = 0.
- **READ & SYNC:** Open `PurchaseOrderListScreen.kt` and confirm the new PO appears with correct `po_number`, vendor, and status. Tap into `PODetailScreen.kt` and confirm the line item and totals render exactly as saved.
- **UPDATE & SYNC:** From `PODetailScreen.kt`, change the PO status (e.g., Draft → Approved/Sent — use whichever exact status transition button is rendered per Phase 1 item 25). Verify ONLY the `status` column changes in `purchase_orders`; `total_amount` and item rows unchanged. Then edit the line item `Qty` back on the create/edit path if supported, and verify `purchase_order_items.ordered_qty` and the parent `purchase_orders.total_amount` both recompute correctly.
- **DELETE & SYNC:** Cancel/delete the PO (use the exact cancel action available in `PODetailScreen.kt`). Verify the row's status/soft-delete flag changes appropriately in `purchase_orders`, and confirm the PO no longer appears as "receivable" when starting a new GRN (Module 8).
- **Edge case — currency field:** Verify the `currency` column in `purchase_orders` populates with the expected currency code and does not silently default incorrectly when the store's currency setting differs from a hardcoded default.

### Module 8 — Goods Receipt Note / GRN (Screens: `CreateGrnScreen.kt`, `GrnListScreen.kt`, `GrnDetailScreen.kt`, `GrnSuccessScreen.kt` | Sheet Tabs: `grn_headers`, `grn_items`)

- **Field/Variable Level Test:** Start a GRN against the Purchase Order created in Module 7. For the line item being received, input `Received Qty` = "18" (intentionally less than the ordered 20, to test partial receipt), confirm/adjust `Category`, `Brand`, `Selling Price`, `Batch / Lot Number`, `Mfg Date`, `Exp Date`. Input header-level `Delivery Notes` = "Automated GRN test — partial receipt".
- **CREATE & SYNC:** Submit the GRN. Verify a new row in `grn_headers` with `po_id`/`po_number` matching the source PO, `vendor_id`/`vendor_name` matching, `total_received_qty` = 18, `total_items` = the count of distinct SKUs received, `status` reflecting a "received"/"partial" state, `notes` = "Automated GRN test — partial receipt". Verify a matching row in `grn_items` with `received_qty` = 18, `ordered_qty` = 20 (carried from the PO item), `batch_number`, `manufacturing_date`, `expiry_date` matching what was entered, `inventory_action` populated, and `is_new_product` correctly reflecting whether this SKU already existed.
- **CROSS-SYNC CHECK (critical):** Verify the receipt actually posted stock into `Inventory` — the item's `stock_qty` in the `Inventory` sheet must increase by exactly 18, and — if `Batch / Lot Number` differs from an existing batch — a new row must appear in `product_batches` with `stock_qty` = 18 and the entered `cost_price`/`selling_price`/dates.
- **READ & SYNC:** Confirm `GrnListScreen.kt` lists the new GRN, and `GrnDetailScreen.kt` shows the exact received quantities and batch info entered. Confirm `GrnSuccessScreen.kt` appears immediately after submission and its `Return to PO List` button navigates back to `PurchaseOrderListScreen.kt` showing the PO now in a "partially received" state (since 18 of 20 were received).
- **UPDATE & SYNC:** Verify whether a submitted GRN can be edited at all. If not, document "write-once, no UPDATE path" explicitly rather than skipping.
- **DELETE & SYNC:** Verify whether a submitted GRN can be voided/deleted, and if so, whether voiding it correctly reverses the `Inventory.stock_qty` and `product_batches.stock_qty` increases it caused. This reversal logic is a high-risk area — test explicitly for double-decrement or silent no-op bugs.
- **Edge case — remaining PO balance:** Start a second GRN against the same PO for the remaining 2 units. Verify the PO status transitions to fully-received and that attempting to receive further against this PO is blocked or clearly flagged.

### Module 9 — CRM / Accounts / Khata (Screen: `CrmScreen.kt` | Sheet Tabs: `Customers`, `KhataEvents`)

- **Field/Variable Level Test — New Customer:** Via `Search Customers` → add-new path, input the customer's `Name *`, `Phone *`, `WhatsApp (optional)` (these labels are shared with `PaymentDialog.kt`'s inline customer-add form — verify both entry points write to the same `Customers` schema).
- **CREATE & SYNC:** Save. Verify new row in `Customers`: `name`, `phone`, `whatsapp`, `loyalty_points` = 0, `lifetime_spend` = 0.
- **READ & SYNC:** Search for the new customer by phone via `Search Customers`; confirm it resolves.
- **UPDATE & SYNC:** Edit `email`/`address` fields (accessible from the customer edit form) and verify only those cells update in `Customers`.
- **DELETE & SYNC:** Delete the customer and verify `is_deleted` = 1 in `Customers`.
- **Khata / ledger sub-flow (`KhataEvents` sheet):** Field-level test: input `Amount` and `Note (Optional)`, then tap `Add Credit (-)`. Verify a new row in `KhataEvents` with `event_type`/`type` reflecting a debit/credit event, `amount` matching, `customer_id` matching, and the parent customer's running balance updates. Repeat with `Record Payment (+)` and verify a second `KhataEvents` row is appended (this is an append-only ledger — verify no prior row is mutated) and the balance nets correctly. Tap `WhatsApp Statement` and verify it generates a statement reflecting the exact sequence of `KhataEvents` rows for that customer, then verify the `Save`/`Cancel` dialog buttons behave correctly (Cancel must discard the pending Khata entry without writing to the sheet).

### Module 10 — Expenses (Screen: `ExpenseScreen.kt` | Sheet Tab: `Expenses`)

- **Field/Variable Level Test:** Input `Amount` = "45.00", `Description` = "Automated test expense", and select a `category` value (verify what category selector exists — chip list or dropdown — and name the exact options shown).
- **CREATE & SYNC:** Save. Verify new row in `Expenses`: `amount` = 45.00, `description` = "Automated test expense", `category` matching selection, `logged_by_user_id` = current user, `timestamp` populated.
- **READ & SYNC:** Confirm the expense appears in the day's expense list and is reflected in `ZReportScreen.kt`'s `Pay Out (Removed)` or expense total if the app nets expenses against the till.
- **UPDATE & SYNC:** Verify whether an edit path exists for a posted expense; if none, document as write-once.
- **DELETE & SYNC:** Tap `Cancel`/delete icon button on the expense row and verify `is_deleted` = 1 in `Expenses`, and that the deleted expense no longer contributes to `ZReportScreen.kt` totals.

### Module 11 — Transaction History / Sales (Screen: `HistoryScreen.kt` | Sheet Tab: `Sales`, driven by POS checkout on `HomeScreen.kt` + `PaymentDialog.kt`)

- **Field/Variable Level Test (creating the source Sale via POS):** On `HomeScreen.kt`, search and add "TEST-ITEM-01" to cart with `Qty (${item.unit})` = "2". Open payment via `PaymentDialog.kt` and split payment across `Cash` = partial amount and `Card` = remainder (test the split-payment fields explicitly, not just single-method).
- **CREATE & SYNC:** Complete the sale. Verify a new row in `Sales` with `items_json` containing "TEST-ITEM-01" at qty 2, `subtotal`, `tax`, `discount`, `total` all arithmetically correct, `payment_method` reflecting "split" (or the app's exact split indicator), `payment_split_json` populated, `cash_amount` and `card_amount` matching the amounts entered, `wallet_amount` = 0, `udhaar_amount` = 0, `customer_id` blank (no customer selected) or matching if one was, `cashier_id` = logged-in user, `sync_uuid` populated and unique, `pos_terminal_id` correct.
- **READ & SYNC:** In `HistoryScreen.kt`, use `Search by Invoice ID or QR UUID` with the new sale's `sync_uuid`/`invoice_id`. Confirm it resolves and line items match. Tap `Load More` on the list and confirm pagination doesn't duplicate or skip transactions.
- **UPDATE & SYNC:** Sales should not be directly editable post-completion — confirm no edit path exists on `HistoryScreen.kt`. Test the `Dismiss` button only dismisses a UI banner/dialog, not the record.
- **DELETE & SYNC:** Confirm there is no delete affordance for a completed sale (financial record integrity) — a sale can only be voided via the `ReturnsScreen.kt` flow (Module 12), never hard/soft-deleted directly from History.
- **Edge case — every payment method individually:** Repeat CREATE with `Cash` only, `Card` only, `Digital Wallet` only, and `UDHAAR` only (Khata credit sale). For the `UDHAAR` case, verify a corresponding `KhataEvents` row is also created (cross-module dependency) and the customer's ledger balance increases.

### Module 12 — Returns & Refunds (Screen: `ReturnsScreen.kt`, reads `Sales`, writes back to `Inventory`/`wastage_log`)

- **Field/Variable Level Test:** Input `Scan QR or Enter Invoice UUID` using the `sync_uuid` from the Sale created in Module 11.
- **READ & SYNC:** Verify the original sale's line items load correctly for selection.
- **Return-to-inventory sub-flow:** Select "TEST-ITEM-01" qty 1, tap `Return to Inventory`. Verify `Inventory.stock_qty` for that item increases by 1, and verify a corresponding `StockAdjustments` row is created with `adjustmentType` = `SALE_RETURN` (cross-check against Module 4's exact enum value).
- **Mark-as-wastage sub-flow:** For a second unit, tap `Mark as Wastage`. Verify a new row appears in `wastage_log` with `reason` = a value consistent with `WastageReason` (Module 13), `quantity` matching, `totalLoss` computed as `quantity × costPrice`, and confirm `Inventory.stock_qty` is NOT incremented (since the item is scrapped, not restocked) — this distinction (`Return to Inventory` increases stock, `Mark as Wastage` does not) is a critical behavioral test, not a formality.
- **Refund sub-flow:** Tap `Issue Refund`. Verify the refund is recorded against the original `Sales` row (check for a `reference_id` linkage or a new negative-total `Sales` row referencing the original `invoice_id`/`sync_uuid`) and that the till/Z-Report totals in Module 15 reflect the refund as a deduction.
- **DELETE & SYNC:** Tap `Dismiss` and `Cancel` and confirm both only close the dialog/banner without partially applying the return.

### Module 13 — Wastage Entry (Screen: `WastageLogScreen.kt` | Sheet Tab: `wastage_log`)

- **Field/Variable Level Test:** Use `Search Product` to select "TEST-ITEM-01". Open `Reason` dropdown and verify all four exact values are present: `EXPIRED`, `DAMAGED`, `THEFT`, `OTHER`. Select `DAMAGED`. Input `Quantity ($selectedUnit)` = "3", `Notes (optional)` = "Automated wastage test".
- **CREATE & SYNC:** Submit. Verify new row in `wastage_log`: `productName` = "TEST-ITEM-01", `quantity` = 3, `reason` = "DAMAGED", `notes` = "Automated wastage test", `totalLoss` = 3 × the item's `cost_price`, `loggedBy` = current user, `wastageDate` = today (YYYY-MM-DD). Verify `Inventory.stock_qty` decreases by 3.
- **READ & SYNC:** Filter the wastage list by the `All` chip and confirm the new entry appears; if reason-specific filter chips exist, verify filtering by `DAMAGED` isolates it correctly.
- **UPDATE & SYNC:** Verify whether wastage entries can be edited post-submission; document "no UPDATE path" if none exists.
- **DELETE & SYNC:** Verify whether a delete/void action exists for a wastage entry, and if so, whether voiding it restores `Inventory.stock_qty` by the same amount it removed.
- **Repeat for `EXPIRED` and `THEFT` reasons** with distinct quantities to confirm each reason value writes correctly and stock decrements consistently regardless of reason.

### Module 14 — Time Clock (Screen: `PunchClockScreen.kt` | Sheet Tab: `Time_Clock`)

- **Field/Variable Level Test:** No free-text fields visible in the scan — this screen is action-button driven (Clock In / Clock Out per Phase 1 item 35). Confirm exact button labels by inspecting the rendered screen (Phase 1 lists 2 buttons; name them precisely once observed, e.g. "Clock In" / "Clock Out" — do not assume, verify against the live UI).
- **CREATE & SYNC (Clock In):** Tap the clock-in button. Verify a new row in `Time_Clock` with `employee_email`/`employee_name` = current user, `event_type` = the clock-in event string, `timestamp` = now, `pos_terminal_id` correct.
- **CREATE & SYNC (Clock Out):** Tap the clock-out button. Verify a second new row (not an update to the first) in `Time_Clock` with `event_type` = the clock-out event string and a later `timestamp`. Confirm this is an append-only event log, not a single mutable shift record.
- **READ & SYNC:** Reopen the screen and confirm the current clock state (in/out) correctly reflects the most recent event.
- **UPDATE / DELETE & SYNC:** Confirm no edit or delete UI exists for punch records (audit-integrity requirement) — explicitly verify absence rather than assuming it.

### Module 15 — Open/Close Register & Z-Report/Day Close (Screens: `TillOpenScreen.kt`, `ZReportScreen.kt` | Sheet Tab: `till_sessions`)

- **Field/Variable Level Test (Open):** Input `Opening Cash (Amount)` = "200.00", `Notes (optional)` = "Automated till open test". Tap the open-register button.
- **CREATE & SYNC:** Verify a new row in `till_sessions` with `openingCash` = 200.00, `status` = "OPEN", `shiftDate` = today, `cashierId`/`cashierName` = current user, `openedAt` populated.
- **Mid-session activity:** Complete at least one Cash sale, one Card sale, and one `UDHAAR` sale (reuse Module 11), and one Expense (Module 10) marked as a pay-out if the app treats expenses as till pay-outs.
- **READ & SYNC (Z-Report screen):** Open `ZReportScreen.kt` mid-shift. Verify the read-only fields compute correctly and match the till session: `Opened At` = the session's `openedAt`, `Opening Cash` = 200.00, `Cash Sales` = sum of cash-portion sales made this session, `Expected in Drawer` = `Opening Cash + Cash Sales + Pay In − Pay Out`, `Total Transactions` = count of sales this session.
- **Field/Variable Level Test (Close):** Input `Physical Cash Count ($currencySymbol)` with a value intentionally different from `Expected in Drawer` (e.g., short by 5.00, to test variance calculation), and test `Pay In (Added)` / `Pay Out (Removed)` fields with nonzero values before closing.
- **UPDATE & SYNC:** Tap `CLOSE DAY (Z-REPORT)`. Verify the `till_sessions` row (same `sessionId`, not a new row) updates: `status` → "CLOSED", `closingCash` = the physical count entered, `expectedCash` matches the computed figure, `netCash` = `closingCash − expectedCash`, `cashVariance` = `physicalCashCount − expectedCash` (verify these two variance-style fields are computed as documented in code comments and are not accidentally swapped), `totalPayIn`/`totalPayOut` matching entries, `closedAt` populated.
- **DELETE & SYNC:** Confirm no delete path exists for a closed till session (financial audit trail).
- **Edge case:** Attempt to make a sale after the register is closed and confirm the app blocks it or forces a new till session to be opened first.

### Module 16 — Admin Dashboard / Users & Permissions (Screen: `AdminAndUsersScreen.kt` | Sheet Tab: `Users_Permissions`)

- **Field/Variable Level Test:** Tap `+ Add User`. Input `Email *` = "testuser@example.com", `Display Name` = "Test User". Select a `Role: $role` value from the role picker (name the exact roles offered once observed on-device, e.g. Admin/Cashier/Manager — verify against source rather than assuming).
- **CREATE & SYNC:** Tap `Save`. Verify new row in `Users_Permissions`: `email` = "testuser@example.com", `name` = "Test User", `role` matching selection, `password_hash` (verify this is never populated with a plaintext password from the UI — should be empty or a proper hash, per the entity's `"password_hash" to ""` default), `permissions_json` reflecting the selected role's permission set.
- **READ & SYNC:** Tap `Users` and confirm the new user is listed with correct role.
- **UPDATE & SYNC:** Edit the user's `Role: $role` to a different role. Verify only `role`/`permissions_json` change in the sheet row; `email` unchanged.
- **DELETE & SYNC:** Tap the `🗑` icon button for "Test User", confirm via dialog. Verify `is_deleted` = 1 in `Users_Permissions` and that this user can no longer sign in / no longer appears in any user picker.
- **Security edge case:** Attempt to delete or downgrade the currently-signed-in admin account (self-lockout scenario) and confirm the app prevents removing the last remaining admin.

### Module 17 — Settings (Screen: `SettingsScreen.kt` — privacy/options)

- **Field/Variable Level Test:** Input `Sheet ID or URL` and `Folder name`, tap `Connect`. Separately, test the PIN sub-flow: `Manage PIN` → input `Current PIN`, `New PIN`, `Confirm New PIN` (and `New 4-Digit PIN` for first-time setup) → confirm `Save`/`Remove PIN` behavior.
- **CREATE/UPDATE & SYNC:** Verify a valid `Sheet ID or URL` correctly re-points the app's active spreadsheet connection (check `AppSetupPrefs`/local prefs update, not a Sheets row — this is local device config, not synced data). Verify the 4 switches on this screen (name each by its adjacent label once observed on-device) correctly toggle their associated local preference and persist across app restart.
- **PIN sub-flow validation:** Set a PIN via `New 4-Digit PIN` → `Save PIN`. Force the app to lock and confirm `PINUnlockScreen.kt`'s `Enter 4-Digit PIN` correctly accepts the matching PIN and rejects a wrong one. Then use `Manage PIN` → `Current PIN` + `New PIN` + `Confirm New PIN` to change it, and confirm the old PIN no longer works. Then use `Remove PIN` and confirm the app no longer prompts for a PIN on next lock/unlock.
- **DELETE & SYNC:** Not applicable — this is a config screen, not an entity CRUD screen; confirm no destructive data-wipe action is accidentally exposed here.

### Module 18 — Hardware (Screens: `HardwareDiagnosticScreen.kt`, `PrinterSettingsScreen.kt`, `BarcodeScannerScreen.kt`) — local device config, not synced

- **Field/Variable Level Test:** Input `MAC Address (e.g. 00:11:22:33:44:55)` with a valid-format Bluetooth MAC, and `IP Address (e.g. 192.168.1.100)` with a valid-format network printer IP.
- **Validation:** Tap `Test Bluetooth Connection` with a deliberately malformed MAC (e.g. missing colons) and confirm the app validates format before attempting connection rather than crashing. Tap `Test Network Connection` with a deliberately unreachable IP and confirm a timeout/error state is shown, not an indefinite spinner.
- **Persistence:** Confirm both fields persist to local prefs after app restart (this is local hardware config, not a Sheets-synced entity — do not expect a sheet row for this module).
- **`Test ML Scanner`** button: verify it opens the on-device camera scanner pipeline (`BarcodeScannerScreen.kt`/`InlineCameraBox.kt`) and returns a scanned value back to the settings/printer screen correctly.

### Module 19 — Sync & Setup (Screens: `SheetPickerScreen.kt`, `SheetSelectionScreen.kt`, `OnboardingScreen.kt`, `SignInScreen.kt`; Workers: `SyncWorker`, `DeltaSyncManager`, `RestoreWorker`, `NightlyBackupWorker`, `AutoLocalBackupWorker`, `MonthlyShardWorker`, `ExpiryCheckWorker`)

- **Field/Variable Level Test:** None (OAuth/account-picker driven). Tap `Continue with Google` on `SignInScreen.kt`, complete Google OAuth, and land on `SheetSelectionScreen.kt`.
- **CREATE & SYNC (new install/new business):** Complete onboarding to create a brand-new spreadsheet. Verify `createSpreadsheet` (Phase 1.B endpoint #1) is called, and verify tabs matching every table name in section 1.A are created via `sheetsBatchUpdate` (endpoint #5) with correct header rows matching each entity's `toSyncMap()` keys exactly.
- **RESTORE & SYNC (existing device / reinstall):** On `SheetPickerScreen.kt`, select an existing spreadsheet. This is the flow flagged as previously broken (reverse sync) — explicitly verify: (a) the screen does NOT navigate to Home until `DeltaSyncManager`'s restore-pull fully completes, (b) a visible progress indicator is shown during restore, (c) killing the app process mid-restore and relaunching resumes or safely restarts the restore rather than landing on a half-populated Home screen, (d) any restore error is both shown to the user via `Retry Restore` and logged via `AppLogger`/visible in `SystemLogsScreen.kt`.
- **Force Sync (menu item on `AdvancedMenuSheet.kt`):** Trigger it while offline and confirm a clear offline error state (not a silent failure) via `SyncWorker`. Trigger it online after making several local changes across multiple modules and confirm all pending `sync_status = "pending"` rows across every table in 1.A flip to `"synced"` and are correctly present in their respective sheet tabs.
- **Background workers:** For `NightlyBackupWorker`/`AutoLocalBackupWorker`, verify a local backup file is produced on schedule (or when manually triggered for test purposes) without blocking the UI thread. For `ExpiryCheckWorker`, verify it correctly flags items whose `expiry_date` falls within `expiry_alert_days` and that this matches the `⏰ Expiring` chip count in Module 1. For `MonthlyShardWorker`, verify large tables (e.g. `Sales`) get correctly sharded/archived per its intended logic without data loss — re-count total rows before and after a shard operation.


## PHASE 3 — DEEP EDGE-CASE & ERROR AUDIT

> For every field named below, Hermes must execute the exact negative test and record actual behavior (crash / silent fail / validation message / accepted-when-it-shouldn't-be). Do not generalize with "test invalid input on all fields" — the list below is the complete set to run individually.

### 3.A Empty-Field Submission Tests
- What happens if `Product Name *` on `InventoryCrudScreen.kt` is submitted empty? (expect a blocking validation message, since it's marked `*`)
- What happens if `SKU *` is submitted empty?
- What happens if `Main Category *` is left unselected and Save is tapped?
- What happens if `Batch Number *` is submitted empty while `Exp Date *` is filled, and vice versa?
- What happens if `Exp Date *` is left empty?
- What happens if `Name *` and `Phone *` on `VendorManagementScreen.kt` are submitted empty?
- What happens if `Name *` and `Phone *` on the inline new-customer form in `PaymentDialog.kt` are submitted empty?
- What happens if `Category Name *` on `CategoryManagementScreen.kt` is submitted empty?
- What happens if `Reason *` on `StockAdjustmentScreen.kt` is submitted empty?
- What happens if `Vendor Name *` and `Phone *` on `CreatePurchaseOrderScreen.kt` are submitted empty, or a PO is saved with zero line items?
- What happens if `Stock Qty *` on the add-batch form in `BatchListBottomSheet.kt` is submitted empty?
- What happens if `Email *` on `AdminAndUsersScreen.kt` is submitted empty, or `Save` is tapped with no `Role` selected?
- What happens if `Opening Cash (Amount)` on `TillOpenScreen.kt` is left empty and the open-register button is tapped?
- What happens if `Physical Cash Count ($currencySymbol)` on `ZReportScreen.kt` is left empty when attempting `CLOSE DAY (Z-REPORT)`?
- What happens if `Amount` on `ExpenseScreen.kt` or the Khata `Amount` field on `CrmScreen.kt` is submitted empty?
- What happens if `Quantity ($selectedUnit)` on `WastageLogScreen.kt` is submitted empty, or `Search Product` has no product selected?
- What happens if the cart on `HomeScreen.kt` is empty and checkout is attempted?

### 3.B Wrong Data-Type / Malformed Input Tests
- `Cost Price`, `Selling Price`, `Tax %`, `Current Stock`, `Low Alert`, `Damaged Qty` (InventoryCrudScreen): input non-numeric text (e.g. "abc"), negative numbers (e.g. "-5"), and an excessively large number (e.g. "999999999999"). Confirm the numeric keyboard/`KeyboardType.Decimal` constraint is enforced and non-numeric characters cannot even be typed, and confirm negative values are rejected where they should be (e.g. `Selling Price` should never be negative) but allowed where the app intends signed input (`Quantity Change (use − for negative)` on `StockAdjustmentScreen.kt` explicitly permits a minus sign — confirm it parses "-5" correctly and rejects non-numeric text like "-abc").
- `Expiry Alert (Days Before)`: input "0", a negative number, and a non-integer decimal like "2.5" — confirm the app coerces/rejects appropriately.
- `Mfg Date`, `Exp Date *`, `Expected Delivery Date (YYYY-MM-DD)`, `Batch/GRN Mfg/Exp Date`, `Wastage Date`: input a malformed date string (e.g. "13/45/2026" or free text "soon"), and input an `Exp Date` earlier than `Mfg Date` — confirm the app validates chronological order and rejects impossible calendar dates rather than silently storing a bad string that will break sheet-side date parsing.
- `Phone *` / `Phone` (Vendor, Customer, User forms): input a non-numeric string, and a string with letters mixed in (e.g. "12ab34"). Confirm behavior — does the app enforce phone format, and does the `WhatsApp Number (with country code)` field on `ReceiptScreen.kt` require a leading `+`/country code, or silently fail to open WhatsApp if malformed?
- `Email *` (AdminAndUsersScreen), `Email` (CustomerEntity/VendorManagementScreen): input a string with no "@" (e.g. "notanemail") and confirm validation triggers before Save is enabled/accepted.
- `Sheet ID or URL` (SettingsScreen): input a completely invalid string (not a URL, not a valid Sheet ID format) and tap `Connect`. Confirm a clear error is surfaced rather than the app attempting the connection indefinitely or crashing.
- `MAC Address` / `IP Address` (PrinterSettingsScreen): input malformed values (e.g. MAC without colons, IP with a value >255 in an octet) and confirm the "Test..." buttons handle this gracefully.
- `Qty (${item.unit})` on `HomeScreen.kt` cart lines: input "0", a negative number, and a value greater than `current_stock` for that item. Confirm zero/negative quantities are rejected and confirm whether the app blocks overselling beyond available stock or allows negative inventory (document actual behavior either way — this is a business-logic-critical finding).
- `New 4-Digit PIN` / `Current PIN` / `New PIN` / `Confirm New PIN`: input fewer than 4 digits, more than 4 digits, and non-numeric characters. Confirm a mismatched `Confirm New PIN` is rejected with a clear message rather than silently overwriting the PIN.
- `Print Quantity` / `Width` / `Height` on `BarcodePrintSettingsScreen.kt`: input "0" and a negative number; confirm the print preview does not attempt to render a zero/negative-size label.
- `Credit Limit` on `VendorManagementScreen.kt`: input a negative value and confirm rejection.

### 3.C Google Sheets / Sync Timeout & Failure Handling
- Simulate the Google Sheets API becoming slow/unresponsive during a `CREATE & SYNC` action (e.g. saving a new Inventory item while network is throttled to a near-timeout speed). Confirm the app shows a clear pending/syncing state, that the local Room row is written immediately with `sync_status = "pending"`, and that the UI does not block or freeze waiting on the network call.
- Simulate a full network loss immediately after tapping Save on any CREATE flow (Modules 1–17). Confirm the record persists locally with `sync_status = "pending"` and is NOT lost, then confirm that when connectivity returns (either automatically or via `Force Sync`), the pending record correctly syncs to its Sheet tab exactly once (verify no duplicate row is created on retry — check whether the sync layer is idempotent using `system_row_id`/primary key matching, or whether a retried `appendValues` call could double-append).
- Simulate an HTTP error response (e.g. 401 Unauthorized from an expired OAuth token) during a sync operation. Confirm `OAuthTokenManager.kt`'s refresh path is triggered and the operation is retried automatically rather than requiring the user to manually re-authenticate for every single sync call; confirm if refresh itself fails, the user is routed to `Logout & Login via OAuth` (the exact button on `PINUnlockScreen.kt`) or an equivalent clear re-auth prompt, not a generic crash.
- Simulate a 429 (rate limit) response from the Sheets API during a bulk `Force Sync` with many pending rows queued. Confirm the app backs off/retries rather than dropping the remaining queued rows.
- Kill the app process mid-sync (during any CREATE/UPDATE/DELETE sync call) and relaunch. Confirm the interrupted record's `sync_status` correctly reflects "pending" (not falsely "synced") and that it is picked up and retried by `SyncWorker` on next run, without creating a duplicate or corrupt row.
- For the restore-on-new-device path specifically (`SheetPickerScreen.kt` → `DeltaSyncManager`, flagged as a previously-broken area): explicitly re-verify the timeout/slow-response case — a very large spreadsheet (many rows across all 19 tabs) should show incremental progress, must not silently truncate data, and a timeout mid-restore must be retryable via `Retry Restore` without leaving the local database in a half-populated state that the app then treats as "fully synced."
- For `DELETE & SYNC` flows specifically: simulate a network failure right after a soft-delete is applied locally (`is_deleted = 1` written to Room). Confirm the deletion still propagates to the Sheet on next successful sync, and confirm the item does not "come back" (i.e., does not get re-created or its `is_deleted` flag does not get reset to 0) due to a stale read overwriting the pending local delete during that sync window.

### 3.D Cross-Module Referential Integrity Edge Cases
- Delete a Category (Module 2) that still has Inventory items assigned to it, then reopen those items — confirm the app does not crash on a dangling `category` reference and displays something sensible.
- Delete a Vendor (Module 6) that has open Purchase Orders (Module 7) referencing it — confirm `PODetailScreen.kt` still renders the PO with the vendor's last-known name/phone rather than crashing on a missing lookup.
- Delete a Customer (Module 9) that has `KhataEvents` ledger history — confirm the ledger history is preserved (or explicitly and correctly archived) rather than silently orphaned or deleted along with the customer.
- Delete a Product/Inventory item (Module 1) that has open `product_batches`, pending `purchase_order_items`, or Sales history referencing it — confirm none of Modules 1, 7, 8, or 11 crash when resolving the now-deleted product's name/price for historical display.
- Attempt to receive a GRN (Module 8) against a Purchase Order that was already cancelled/deleted (Module 7) — confirm the app blocks this rather than silently posting stock for a cancelled order.
